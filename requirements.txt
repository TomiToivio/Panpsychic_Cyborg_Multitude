# Panpsychic Cyborg Multitude — minimal/core installation
#
# Python 3.11+. Optional integrations are declared as extras in pyproject.toml.
pydantic>=2.0

# Required for real nodes: did:key identity + signed envelopes.
cryptography>=42.0
base58>=2.1.1

# Optional installs:
#   pip install -e '.[zenoh]'   # node-to-node networking fabric
#   pip install -e '.[dev]'     # test/development dependencies
#   pip install -e '.[all]'     # all currently packaged optional extras
